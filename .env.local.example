# ── Supabase (supabase.com → project settings → API) ─────────────────
NEXT_PUBLIC_SUPABASE_URL=https://<project-ref>.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJ...
SUPABASE_SERVICE_ROLE_KEY=eyJ...          # server only, never expose

# ── Google Cloud OAuth client (console.cloud.google.com) ─────────────
# Enable the Gmail API; consent screen in Testing mode with your email
# as a test user; redirect URIs must include GOOGLE_REDIRECT_URI below
# and https://<project-ref>.supabase.co/auth/v1/callback
GOOGLE_CLIENT_ID=xxxx.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=GOCSPX-...
GOOGLE_REDIRECT_URI=http://localhost:3000/api/gmail/callback

# ── Gemini (aistudio.google.com → Get API key) ───────────────────────
GEMINI_API_KEY=AIza...

# ── Google Places API (console.cloud.google.com → enable "Places API (New)") ──
# Used to fetch a real photo for each suggested venue. Can be the same
# Google Cloud project as the OAuth client above; create an API key under
# Credentials and restrict it to the Places API.
GOOGLE_PLACES_API_KEY=AIza...

# ── App secrets ──────────────────────────────────────────────────────
TOKEN_ENCRYPTION_KEY=                     # openssl rand -hex 32
CRON_SECRET=                              # any long random string
NEXT_PUBLIC_APP_URL=http://localhost:3000
